apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "personal-resume.fullname" . }}-test-connection"
  labels:
    {{- include "personal-resume.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "personal-resume.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
