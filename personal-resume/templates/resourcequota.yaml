# templates/resourcequota.yaml
apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ .Values.name }}-quota
spec:
  hard:
    pods: "{{ .Values.replicaCount }}"
    requests.cpu: {{ .Values.container.cpuReq }}
    requests.memory: {{ .Values.container.memoryReq }}
    limits.cpu: {{ .Values.container.cpuLim }}
    limits.memory: {{ .Values.container.memoryLim }}